/*9) Qual o objetivo do algoritmo representado no trecho de código a seguir.   */
    do
        {
            printf("Informe um valor positivo: ");
            scanf("%f",&Num);
          if (Num < 0)
        {
            Tentativas++;
        }
             }while(Num < 0);

//Pedir ao usuário que informe um valor positivo e contar a quantidade de tentativas necessárias para atingir o objetivo.
